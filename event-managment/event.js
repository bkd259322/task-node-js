const mongoose = require("mongoose");

const { event } = require("./eventSchema");
const { speaker } = require("./speakerSchema");
const { session } = require("./sessionSchema");
const { vandor } = require("./vendorSchema");
const { attendedandtickettype } = require("./attendee");
const { ticket } = require("./ticket_type");
const{organizer} = require("./organizersSchema")
